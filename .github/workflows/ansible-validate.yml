name: Ansible Validation

on:
  push:
    branches: [main, develop]
    paths:
      - 'ansible/**'
      - '.github/workflows/ansible-validate.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'ansible/**'

jobs:
  # Fast validation jobs run in parallel
  quick-checks:
    name: Quick Checks (Syntax, YAML, Inventory)
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install tools with UV
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          uv pip install --system --break-system-packages ansible yamllint

      - name: Check Ansible version
        run: ansible --version

      - name: Install Ansible Galaxy dependencies
        run: |
          cd ansible
          ansible-galaxy install -r requirements.yml
        continue-on-error: true

      - name: Create dummy vault password
        run: echo "dummy_password_for_ci_only" > ~/.ansible_vault_pass

      - name: Ansible Syntax Check
        run: |
          cd ansible
          ansible-playbook playbooks/site.yml --syntax-check

      - name: YAML Lint
        run: |
          yamllint -c .yamllint.yml ansible/
        continue-on-error: true

      - name: Validate inventory structure
        run: |
          cd ansible
          ansible-inventory -i inventory/hetzner.yml --list || true

  # Ansible lint runs separately (slower)
  ansible-lint:
    name: Ansible Lint
    runs-on: ubuntu-slim

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install ansible-lint with UV
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          uv pip install --system --break-system-packages ansible ansible-lint

      - name: Run ansible-lint
        run: |
          cd ansible
          ansible-lint playbooks/ roles/ -v
        continue-on-error: true

      - name: Generate lint report
        if: always()
        run: |
          cd ansible
          ansible-lint playbooks/ roles/ --parseable > ../ansible-lint-report.txt || true

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ansible-lint-report
          path: ansible-lint-report.txt

  summary:
    name: Validation Summary
    runs-on: ubuntu-slim
    needs: [quick-checks, ansible-lint]
    if: always()

    steps:
      - name: Validation Summary
        run: |
          echo "## Ansible Validation Results ⚙️" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Quick Checks | ${{ needs.quick-checks.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Ansible Lint | ${{ needs.ansible-lint.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "ℹ️ **Note:** Molecule tests run in the main CI workflow" >> $GITHUB_STEP_SUMMARY
