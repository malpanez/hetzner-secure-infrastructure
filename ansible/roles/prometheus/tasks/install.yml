---
# Prometheus Role - Installation Tasks (Multi-platform orchestrator)

- name: Prometheus | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  when: prometheus_install_method in ['apt', 'yum', 'dnf']
  tags: [prometheus, install, packages]
