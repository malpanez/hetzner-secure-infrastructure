---
# Nginx WordPress Role - Installation Tasks (Multi-platform orchestrator)

- name: Nginx WordPress | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  tags: [nginx-wordpress, install, packages]
