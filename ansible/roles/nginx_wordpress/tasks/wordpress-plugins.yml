---
# Nginx WordPress Role - WordPress Plugins Installation

- name: Nginx WordPress | Plugins | Check if WordPress is installed
  ansible.builtin.stat:
    path: "{{ nginx_wordpress_web_root }}/wp-config.php"
  register: wp_config_check
  tags: [nginx-wordpress, wordpress, plugins]

# ========================================
# Remove Bloat Plugins
# ========================================

- name: Nginx WordPress | Plugins | Remove bloat plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin delete {{ item }}
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  loop: "{{ nginx_wordpress_plugins_remove | default([]) }}"
  register: plugin_remove
  changed_when: "'Deleted' in plugin_remove.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_remove is defined
    - nginx_wordpress_plugins_remove | length > 0
  tags: [nginx-wordpress, wordpress, plugins, cleanup, molecule-notest]

# ========================================
# Install Plugin Groups
# ========================================

- name: Nginx WordPress | Plugins | Install mandatory infrastructure plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin install
      {{ nginx_wordpress_plugins_mandatory | join(' ') }} --activate
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: plugin_install
  changed_when: "'Installing' in plugin_install.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_mandatory | default([]) | length > 0
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

- name: Nginx WordPress | Plugins | Install operations plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin install
      {{ nginx_wordpress_plugins_operations | join(' ') }} --activate
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: operations_plugin_install
  changed_when: "'Installing' in operations_plugin_install.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_operations | default([]) | length > 0
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

- name: Nginx WordPress | Plugins | Install design plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin install
      {{ nginx_wordpress_plugins_design | join(' ') }} --activate
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: design_plugin_install
  changed_when: "'Installing' in design_plugin_install.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_design | default([]) | length > 0
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

- name: Nginx WordPress | Plugins | Install forms plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin install
      {{ nginx_wordpress_plugins_forms | join(' ') }} --activate
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: forms_plugin_install
  changed_when: "'Installing' in forms_plugin_install.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_forms | default([]) | length > 0
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

- name: Nginx WordPress | Plugins | Install accessibility plugins
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin install
      {{ nginx_wordpress_plugins_accessibility | join(' ') }} --activate
      --path={{ nginx_wordpress_web_root }}
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: accessibility_plugin_install
  changed_when: "'Installing' in accessibility_plugin_install.stdout"
  failed_when: false
  when:
    - wp_config_check.stat.exists
    - nginx_wordpress_plugins_accessibility | default([]) | length > 0
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

# ========================================
# Display Installed Plugins
# ========================================

- name: Nginx WordPress | Plugins | Check plugin installation status
  ansible.builtin.command:
    cmd: >-
      {{ nginx_wordpress_wpcli_path }} plugin list
      --path={{ nginx_wordpress_web_root }} --format=json
  become_user: "{{ nginx_wordpress_nginx_user }}"
  register: installed_plugins
  changed_when: false
  when: wp_config_check.stat.exists
  tags: [nginx-wordpress, wordpress, plugins, molecule-notest]

- name: Nginx WordPress | Plugins | Display installed plugins summary
  ansible.builtin.debug:
    msg: "{{ ['========================================',
             'WordPress Plugins — ' + nginx_wordpress_site_name | upper + ' site',
             '========================================',
             '',
             'INFRASTRUCTURE (3):',
             '  ✓ redis-cache - Valkey object cache',
             '  ✓ nginx-helper - FastCGI cache purging',
             '  ✓ wordfence-login-security - 2FA/MFA for admin',
             '',
             'OPERATIONS (3):',
             '  ✓ wp-mail-smtp - Email (Brevo free 300/day)',
             '  ✓ seo-by-rank-math - SEO',
             '  ✓ cookie-notice - GDPR/CCPA consent',
             '',
             'DESIGN — Kadence stack (Gutenberg-native):',
             '  ✓ kadence-blocks - Advanced blocks (free)',
             '  ✓ kadence-starter-templates - Template importer',
             '  ⚠ kadence-pro - PENDING AUTHORIZATION ($129/year)',
             '',
             'FORMS:',
             '  ✓ fluentform - Lead capture, contact forms',
             '',
             'PAYMENTS (academy — LearnDash built-in Stripe):',
             '  ✓ Stripe Connect — LearnDash → Settings → Payments → Stripe Connect',
             '  ✓ One-time payment per course ($1000+), enrollment automatic on purchase',
             '  ✓ Enable Stripe Tax for automatic EU VAT (+0.5% per transaction)',
             '',
             'ACCESSIBILITY:',
             '  ✓ accessibility-assistant - ADHD/Dyslexia modes',
             '',
             'MANUAL INSTALL REQUIRED (academy):',
             '  ⚠ LearnDash Pro - $199/year (sfwd-lms)',
             '  ○ Kadence Pro Bundle - $129/year (OPTIONAL — Free sufficient for current design)',
             '',
             'REMOVED BLOAT:',
             '  ✗ hello - Hello Dolly',
             '  ✗ akismet - Spam (comments disabled)',
             '',
             '========================================',
             'Annual cost: ~$377/year (LearnDash + Kadence + SureCart)',
             '========================================'] }}"
  when:
    - wp_config_check.stat.exists
    - installed_plugins.stdout is defined
  tags: [nginx-wordpress, wordpress, plugins, info]

# ========================================
# Configure Plugins
# ========================================

- name: Nginx WordPress | Plugins | Configure Redis Object Cache
  block:
    - name: Nginx WordPress | Plugins | Check if Redis/Valkey is running
      ansible.builtin.systemd:
        name: valkey
      register: valkey_status
      failed_when: false
      changed_when: false

    - name: Nginx WordPress | Plugins | Enable Redis Object Cache
      ansible.builtin.command:
        cmd: >-
          {{ nginx_wordpress_wpcli_path }} redis enable
          --path={{ nginx_wordpress_web_root }}
      become_user: "{{ nginx_wordpress_nginx_user }}"
      when: valkey_status.status.ActiveState == 'active'
      register: redis_enable
      changed_when: "'already enabled' not in redis_enable.stdout"
      failed_when: false

    - name: Nginx WordPress | Plugins | Display Redis cache status
      ansible.builtin.debug:
        msg: >-
          Redis Object Cache:
          {{ 'ENABLED ✓' if valkey_status.status.ActiveState == 'active'
             else 'DISABLED (Valkey not running)' }}
  when: wp_config_check.stat.exists
  tags: [nginx-wordpress, wordpress, plugins, redis]

- name: Nginx WordPress | Plugins | Configure Nginx Helper
  block:
    - name: Nginx WordPress | Plugins | Enable Nginx Helper purge on update
      ansible.builtin.command:
        cmd: >-
          {{ nginx_wordpress_wpcli_path }} option update rt_wp_nginx_helper_options
          '{{ nginx_helper_options | to_json }}' --format=json
          --path={{ nginx_wordpress_web_root }}
      become_user: "{{ nginx_wordpress_nginx_user }}"
      vars:
        nginx_helper_options:
          enable_purge: "1"
          cache_method: "enable_fastcgi"
          purge_method: "delete_local_server"
          enable_map: "0"
          enable_log: "0"
          enable_stamp: "0"
      register: nginx_helper_config
      changed_when: nginx_helper_config.rc == 0
      failed_when: false

    - name: Nginx WordPress | Plugins | Display Nginx Helper status
      ansible.builtin.debug:
        msg: "Nginx Helper: FastCGI cache purging ENABLED ✓"
  when: wp_config_check.stat.exists
  tags: [nginx-wordpress, wordpress, plugins, cache]

# ========================================
# Post-Installation Instructions
# ========================================

- name: Nginx WordPress | Plugins | Post-installation instructions
  ansible.builtin.debug:
    msg:
      - "============================================"
      - "Post-Install Checklist — {{ nginx_wordpress_site_name | upper }} site"
      - "============================================"
      - ""
      - "AUTO-CONFIGURED (no action needed):"
      - "  ✓ Redis Object Cache connected to Valkey"
      - "  ✓ Nginx Helper FastCGI cache purging"
      - "  ✓ Cloudflare WAF + Fail2ban + AppArmor"
      - "  ✓ Backups: Ansible role → Hetzner S3"
      - ""
      - "CONFIGURE MANUALLY:"
      - ""
      - "  1. Email (WP Mail SMTP) — Zoho Mail:"
      - "     WP Mail SMTP → Settings → Other SMTP"
      - "     Host: smtp.zoho.eu (EU) or smtp.zoho.com (US)"
      - "     Port: 587 (TLS) | Auth: Yes"
      - "     User: support@twomindstrading.com"
      - "     Pass: Zoho app password (Zoho → Security → App Passwords)"
      - ""
      - "  2. SEO (Rank Math):"
      - "     Rank Math → Setup Wizard → connect Google Search Console"
      - ""
      - "  3. 2FA (Wordfence Login Security):"
      - "     Users → Your Profile → Enable 2FA"
      - ""
      - "  4. GDPR (Cookie Notice):"
      - "     Settings → Cookie Notice → customize colors to match brand"
      - ""
      - "MAIN SITE — design migration:"
      - "  • Kadence Customizer: reproduce existing dark color palette"
      - "  • Use screenshots of current site as reference"
      - "  • Kadence Pro Bundle ($129/year) — install once authorized:"
      - "    Unlocks: Header/Footer builder, advanced sections, WooCommerce"
      - ""
      - "ACADEMY — manual installs (in order):"
      - "  1. LearnDash Pro ZIP ($199/year) — Plugins → Upload"
      - "  2. Kadence Pro Bundle ZIP ($129/year) — OPTIONAL (Free tier sufficient)"
      - "  3. Starter template: Appearance → Kadence → Business Courses"
      - "  4. LearnDash → Settings → Payments → Stripe (one-time per course)"
      - ""
      - "============================================"
  when: wp_config_check.stat.exists
  run_once: true
  tags: [nginx-wordpress, wordpress, plugins, info]
