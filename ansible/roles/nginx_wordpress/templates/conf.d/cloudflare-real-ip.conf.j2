# ============================================================================
# Cloudflare Real IP Configuration
# ============================================================================
# Managed by Ansible - nginx_wordpress role
#
# When using Cloudflare proxy, Nginx sees Cloudflare's IP instead of visitor's IP.
# This configuration extracts the real visitor IP from Cloudflare headers.
#
# CRITICAL for:
# - Rate limiting (per-user instead of per-Cloudflare-IP)
# - fail2ban (ban real attackers, not Cloudflare)
# - Logs and analytics (see real visitor IPs and countries)
#
# Cloudflare IP ranges: https://www.cloudflare.com/ips/
# Update annually or when Cloudflare announces changes
# ============================================================================

{% if nginx_wordpress_cloudflare_enabled | default(true) %}
# Trust these IP ranges to provide real visitor IP
# Only Cloudflare's servers are trusted (prevents IP spoofing)

# IPv4 ranges
set_real_ip_from 103.21.244.0/22;
set_real_ip_from 103.22.200.0/22;
set_real_ip_from 103.31.4.0/22;
set_real_ip_from 104.16.0.0/13;
set_real_ip_from 104.24.0.0/14;
set_real_ip_from 108.162.192.0/18;
set_real_ip_from 131.0.72.0/22;
set_real_ip_from 141.101.64.0/18;
set_real_ip_from 162.158.0.0/15;
set_real_ip_from 172.64.0.0/13;
set_real_ip_from 173.245.48.0/20;
set_real_ip_from 188.114.96.0/20;
set_real_ip_from 190.93.240.0/20;
set_real_ip_from 197.234.240.0/22;
set_real_ip_from 198.41.128.0/17;

# IPv6 ranges
set_real_ip_from 2400:cb00::/32;
set_real_ip_from 2606:4700::/32;
set_real_ip_from 2803:f800::/32;
set_real_ip_from 2405:b500::/32;
set_real_ip_from 2405:8100::/32;
set_real_ip_from 2a06:98c0::/29;
set_real_ip_from 2c0f:f248::/32;

# Use CF-Connecting-IP header for real visitor IP
# Cloudflare sets this header with the original visitor's IP address
real_ip_header CF-Connecting-IP;

# Walk through multiple proxy layers if needed
real_ip_recursive on;
{% endif %}
