---
# Firewall Role - Installation Tasks (Multi-platform orchestrator)

- name: Firewall | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  tags: [firewall, install, packages]
