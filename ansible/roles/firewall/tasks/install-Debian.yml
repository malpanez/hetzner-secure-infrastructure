---
# Firewall Role - Debian/Ubuntu Installation Tasks

- name: Firewall | Install | Install UFW package
  ansible.builtin.apt:
    name: "{{ firewall_apt_packages }}"
    state: present
    update_cache: false
  tags: [firewall, packages]
