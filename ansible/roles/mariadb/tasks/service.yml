---
# MariaDB Role - Service Management Tasks

- name: MariaDB | Service | Enable and start MariaDB service
  ansible.builtin.systemd:
    name: mariadb
    enabled: true
    state: started
    daemon_reload: true
  tags: [mariadb, service]
