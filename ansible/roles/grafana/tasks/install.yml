---
# Grafana Role - Installation Tasks (Multi-platform orchestrator)

- name: Grafana | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  when: grafana_install_method in ['apt', 'yum', 'dnf']
  tags: [grafana, install, packages]
