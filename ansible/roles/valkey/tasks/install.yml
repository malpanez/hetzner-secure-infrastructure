---
# Valkey Role - Installation Tasks (Multi-platform orchestrator)

- name: Valkey | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  tags: [valkey, install, packages]
