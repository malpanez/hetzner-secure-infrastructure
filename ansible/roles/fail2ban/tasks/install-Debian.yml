---
# Fail2ban Role - Debian/Ubuntu Installation Tasks

- name: Fail2Ban | Install | Install Fail2ban package
  ansible.builtin.apt:
    name: "{{ fail2ban_package }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [fail2ban, packages]
