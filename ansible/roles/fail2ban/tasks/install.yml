---
# Fail2ban Role - Installation Tasks (Multi-platform orchestrator)

- name: Fail2Ban | Install | Include OS-specific installation tasks
  ansible.builtin.include_tasks: "install-{{ ansible_os_family }}.yml"
  tags: [fail2ban, install, packages]
