---
phase: "03.6"
plan: "05"
subsystem: nginx_wordpress
tags: [mu-plugins, smtp, repo-hygiene, ansible]
dependency_graph:
  requires: []
  provides: [force-gd.php tracked in repo, SMTP idempotent deployment]
  affects: [nginx_wordpress role, dual-wordpress.yml, academy site]
tech_stack:
  added: []
  patterns: [set_fact + to_json for multi-line JSON in Ansible tasks]
key_files:
  created:
    - ansible/roles/nginx_wordpress/files/force-gd.php
  modified:
    - ansible/roles/nginx_wordpress/tasks/wordpress-mu-plugins.yml
    - ansible/roles/nginx_wordpress/tasks/wordpress-optimization.yml
    - ansible/roles/nginx_wordpress/defaults/main.yml
    - ansible/playbooks/dual-wordpress.yml
decisions:
  - Used set_fact + to_json filter to build SMTP JSON config, avoiding yamllint line-length violation
  - SMTP task placed in wordpress-optimization.yml alongside other WP-CLI option updates
  - force-gd.php scoped to academy site only (consistent with tmt-academy.php pattern)
metrics:
  duration: "~15 min"
  completed_date: "2026-04-19"
  tasks_completed: 3
  files_changed: 5
---

# Phase 3.6 Plan 05: Repo/Code Gaps — force-gd.php, SMTP Ansible Task Summary

Tracked missing server files in repo and added idempotent SMTP configuration via WP-CLI using Ansible.

## Tasks Completed

### Task 1: Track force-gd.php in repo (M3)

Retrieved `force-gd.php` from the academy server. Content:

```php
<?php
add_filter('wp_image_editors', function($editors) {
    return array('WP_Image_Editor_GD', 'WP_Image_Editor_Imagick');
});
```

- Created `ansible/roles/nginx_wordpress/files/force-gd.php`
- Added deploy task to `wordpress-mu-plugins.yml`, scoped to `site_name == 'academy'`

### Task 2: SMTP configuration task for academy

- Added `nginx_wordpress_smtp_*` defaults (host, port, from_email, from_name, user, pass) to `defaults/main.yml`
- Added two tasks to `wordpress-optimization.yml`:
  - `Build WP Mail SMTP config` — uses `set_fact` + `to_json` to construct the config dict cleanly
  - `Set WP Mail SMTP settings` — calls `wp option update wp_mail_smtp` via `argv` (no shell quoting issues)
  - Both tasks: `no_log: true`, guarded by `smtp_host != ''`, tagged `molecule-notest`
- Added academy SMTP vars to `dual-wordpress.yml`: Zoho EU SMTP, `vault_smtp_password` for credentials

### Task 3: ansible-lint verification

- ansible-lint flags pre-existing `tests/test.yml` role-not-found error (unrelated to this plan)
- yamllint passes clean on all 4 changed files (max 250 chars)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] yamllint line-length violation on SMTP command**
- **Found during:** Task 3
- **Issue:** Single-line JSON string exceeded 250 characters
- **Fix:** Replaced inline JSON string with `set_fact` + Jinja2 `to_json` filter; used `argv` list instead of `cmd` string to avoid shell quoting issues
- **Files modified:** `wordpress-optimization.yml`
- **Commit:** 901083b

**2. [Rule 1 - Bug] Extra blank line at end of wordpress-mu-plugins.yml**
- **Found during:** Task 3
- **Issue:** yamllint `empty-lines` error
- **Fix:** Rewrote file removing trailing blank line
- **Files modified:** `wordpress-mu-plugins.yml`
- **Commit:** 901083b

## Known Stubs

None.

## Self-Check: PASSED

- `ansible/roles/nginx_wordpress/files/force-gd.php` — exists
- `ansible/roles/nginx_wordpress/tasks/wordpress-mu-plugins.yml` — force-gd.php task present
- `ansible/roles/nginx_wordpress/tasks/wordpress-optimization.yml` — SMTP tasks present
- `ansible/roles/nginx_wordpress/defaults/main.yml` — SMTP defaults present
- `ansible/playbooks/dual-wordpress.yml` — academy SMTP vars present
- Commit `901083b` — verified
