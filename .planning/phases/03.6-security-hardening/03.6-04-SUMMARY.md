---
phase: "03.6"
plan: "04"
subsystem: security
tags: [fail2ban, nginx, wordpress, file-permissions, hardening]
dependency_graph:
  requires: [fail2ban role, nginx_wordpress role]
  provides: [nginx-limit-req jail, wordpress-auth jail, wp file permissions hardening]
  affects: [fail2ban service on server, /var/www permissions]
tech_stack:
  added: []
  patterns: [fail2ban filter files, Ansible find+chmod pattern]
key_files:
  created:
    - ansible/roles/fail2ban/files/filter-nginx-limit-req.conf
    - ansible/roles/fail2ban/files/filter-wordpress-auth.conf
    - ansible/roles/nginx_wordpress/tasks/wordpress-permissions.yml
  modified:
    - ansible/inventory/group_vars/hetzner/security.yml
    - ansible/roles/fail2ban/defaults/main.yml
    - ansible/roles/fail2ban/tasks/configure.yml
    - ansible/roles/nginx_wordpress/tasks/main.yml
decisions:
  - "Added jails to group_vars/hetzner/security.yml not just role defaults — group_vars overrides defaults, role defaults were shadowed"
metrics:
  duration: "~25 minutes"
  completed: "2026-04-20"
  tasks_completed: 5
  files_modified: 7
---

# Phase 3.6 Plan 04: Fail2ban WordPress/Nginx Jails + File Permissions Summary

Hardened production server with two new fail2ban jails (nginx 429 rate-limit bans and WordPress login brute-force) and enforced correct WordPress file permissions across both sites.

## Tasks Completed

| Task | Description | Status |
|------|-------------|--------|
| 1 | Inspect current fail2ban role | Done |
| 2 | Add nginx-limit-req and wordpress-auth jails | Done |
| 3 | Create wordpress-permissions.yml task file | Done |
| 4 | Apply to server via Ansible + direct SSH | Done |
| 5 | Verify jails active and permissions correct | Done |

## Verification Results

- `fail2ban-client status` shows 3 jails: `nginx-limit-req, sshd, wordpress-auth`
- `find /var/www -name "*.php" -perm /022` returns no results
- `wp-config.php` on both sites: `-rw-r----- root:www-data` (640)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] group_vars overriding role defaults**

- **Found during:** Task 4 (first apply returned changed=0 but jails not active)
- **Issue:** `ansible/inventory/group_vars/hetzner/security.yml` defines `fail2ban_services` with only sshd, overriding the role defaults that had the new jails added. Ansible variable precedence means group_vars beats role defaults.
- **Fix:** Added nginx-limit-req and wordpress-auth jails to `group_vars/hetzner/security.yml` which is the authoritative source for this variable in this project.
- **Files modified:** `ansible/inventory/group_vars/hetzner/security.yml`
- **Commit:** 08ec45d

## Commits

| Hash | Message |
|------|---------|
| 08ec45d | fix(security): add fail2ban WordPress/Nginx jails and fix file permissions (H3 H4) |

## Self-Check: PASSED

- filter-nginx-limit-req.conf: FOUND on server at /etc/fail2ban/filter.d/
- filter-wordpress-auth.conf: FOUND on server at /etc/fail2ban/filter.d/
- wordpress-permissions.yml: FOUND locally
- All 3 jails active in fail2ban (verified via fail2ban-client status)
- wp-config.php 640 root:www-data on both sites (verified)
