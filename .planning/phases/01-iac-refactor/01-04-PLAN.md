---
phase: 01-iac-refactor
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - terraform/modules/cloudflare-config/dns.tf
autonomous: true
requirements: [TF-01]

must_haves:
  truths:
    - "terraform plan shows a new cloudflare_record.academy resource to be created"
    - "The DNS record points academy.twomindstrading.com to the same server IP as the root record"
    - "No existing DNS records are modified or destroyed"
  artifacts:
    - path: "terraform/modules/cloudflare-config/dns.tf"
      provides: "Academy subdomain A record"
      contains: "cloudflare_record.*academy"
  key_links:
    - from: "terraform/modules/cloudflare-config/dns.tf"
      to: "var.server_ipv4"
      via: "content attribute references existing variable"
      pattern: "content = var.server_ipv4"
---

<objective>
Add a Cloudflare DNS A record for academy.twomindstrading.com pointing to the same Hetzner server IP, following the exact pattern of existing A records in dns.tf.

Purpose: The academy subdomain must resolve to the server before Nginx can serve the second WordPress site. This is a Terraform-only change with no Ansible dependency.
Output: Updated dns.tf with a new `cloudflare_record.academy` resource.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-iac-refactor/01-RESEARCH.md
@terraform/modules/cloudflare-config/dns.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cloudflare_record.academy A record to dns.tf</name>
  <files>
    terraform/modules/cloudflare-config/dns.tf
  </files>
  <read_first>
    terraform/modules/cloudflare-config/dns.tf
  </read_first>
  <action>
Add a new `cloudflare_record` resource block to `terraform/modules/cloudflare-config/dns.tf`. Insert it after the `cloudflare_record.prometheus` resource (after line 61, before the Email/Zoho Mail section). Follow the exact pattern of the `cloudflare_record.grafana` and `cloudflare_record.prometheus` A records.

The new resource:

```hcl
resource "cloudflare_record" "academy" {
  zone_id = data.cloudflare_zone.main.id
  name    = "academy"
  content = var.server_ipv4
  type    = "A"
  proxied = true
  ttl     = 1
  comment = "Academy WordPress site (LMS + WooCommerce)"
}
```

Key details:

- Resource name `academy` uses underscore convention (no hyphens)
- `zone_id` references the same data source as all other records
- `content` uses `var.server_ipv4` (same as root, grafana, prometheus)
- `proxied = true` enables Cloudflare CDN + DDoS protection
- `ttl = 1` is Cloudflare "Auto" (required when proxied)
- No new variables needed — `var.server_ipv4` already exists

After adding, run `terraform fmt` on the file to ensure consistent formatting.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/hetzner-secure-infrastructure && grep -A 8 'resource "cloudflare_record" "academy"' terraform/modules/cloudflare-config/dns.tf && cd terraform && terraform fmt -check -recursive 2>&1 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'resource "cloudflare_record" "academy"' terraform/modules/cloudflare-config/dns.tf` returns a match
    - `grep 'name    = "academy"' terraform/modules/cloudflare-config/dns.tf` returns a match
    - `grep 'content = var.server_ipv4' terraform/modules/cloudflare-config/dns.tf` returns at least 4 matches (root + grafana + prometheus + academy)
    - `grep 'type    = "A"' terraform/modules/cloudflare-config/dns.tf` returns at least 4 matches
    - `terraform fmt -check -recursive` exits 0 from the terraform/ directory (no formatting issues)
    - The academy resource block appears AFTER the prometheus block and BEFORE the Email/Zoho section
  </acceptance_criteria>
  <done>dns.tf contains a new cloudflare_record.academy A record pointing to var.server_ipv4 with proxied=true, following the exact pattern of adjacent records, and terraform fmt passes</done>
</task>

</tasks>

<verification>
After task completes:
1. `terraform fmt -check -recursive` passes from the terraform/ directory
2. `terraform validate` passes from `terraform/` (requires provider config, may need `terraform init` first — if not initialized, skip this check and note it for Phase 2 validation)
3. The dns.tf file has exactly 4 A-type `cloudflare_record` resources (root, grafana, prometheus, academy)
</verification>

<success_criteria>

- dns.tf has a properly formatted `cloudflare_record.academy` resource
- No existing records are modified
- The resource follows project conventions (underscore naming, consistent attribute alignment)
- terraform fmt passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-iac-refactor/01-04-SUMMARY.md`
</output>
