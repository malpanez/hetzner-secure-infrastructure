---
phase: 09.1-ansible-roles-optimization-module-defaults-block-consolidation-idiomatic-file-management
plan: 02
subsystem: infra
tags: [ansible, module_defaults, block_rescue, yaml, roles, openbao, valkey, ssh_2fa, cloudflare]

requires:
  - phase: 09.1-ansible-roles-optimization-module-defaults-block-consolidation-idiomatic-file-management
    plan: 01
    provides: "module_defaults pattern established in nginx_wordpress tasks"

provides:
  - module_defaults for owner/group in cloudflare_origin_ssl deploy tasks
  - module_defaults for owner/group/mode in valkey directory and template tasks
  - module_defaults for owner/group in openbao config deploy tasks
  - block/rescue with cleanup in openbao TLS cert generation
  - module_defaults for SSH drop-in templates in ssh_2fa
  - consolidated block-level when conditions for SSH and sudo PAM tasks
  - OPT-06 audit: network modules have no shared-param candidates

affects:
  - 09.1-03

tech-stack:
  added: []
  patterns:
    - "module_defaults block pattern: wrap tasks sharing owner/group/mode in block with module_defaults; add noqa comment on tasks relying on inherited params"
    - "block/rescue for recoverable failures: TLS cert gen cleanup in rescue; re-raise with ansible.builtin.fail"
    - "when consolidation: group tasks sharing identical when conditions into a named block"

key-files:
  created: []
  modified:
    - ansible/roles/cloudflare_origin_ssl/tasks/main.yml
    - ansible/roles/valkey/tasks/configure.yml
    - ansible/roles/openbao/tasks/configure.yml
    - ansible/roles/ssh_2fa/tasks/configure.yml

key-decisions:
  - "valkey configure.yml: directory and template tasks share owner/group/mode in one block; state: directory kept on individual tasks for readability"
  - "openbao configure.yml: TLS block/rescue has standalone when: openbao_tls_enabled at block level; config deploy block uses module_defaults for openbao_user/group with root override on backup script"
  - "ssh_2fa configure.yml: SSH drop-in block uses module_defaults (backup: yes included); PAM tasks consolidated into two named blocks (SSH PAM / Sudo PAM) with block-level when"
  - "OPT-06 audit: all uri/get_url calls in target roles are single-use with no shared auth/headers — no module_defaults candidates found"

patterns-established:
  - "noqa: risky-file-permissions on tasks that rely on module_defaults for owner/group/mode"
  - "block/rescue cleanup pattern: rescue removes partial artifacts then re-raises with ansible.builtin.fail"
  - "named blocks for when consolidation: block name reflects the group purpose (e.g. SSH PAM 2FA configuration)"

requirements-completed: [OPT-01, OPT-02, OPT-05, OPT-06]

duration: 20min
completed: 2026-04-21
---

# Phase 09.1 Plan 02: Infrastructure Service Roles DRY Optimization Summary

**module_defaults + block/rescue applied to cloudflare_origin_ssl, valkey, openbao, and ssh_2fa; PAM tasks consolidated into named blocks; OPT-06 network module audit completed with no candidates**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-04-21T00:00:00Z
- **Completed:** 2026-04-21T00:20:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- cloudflare_origin_ssl/main.yml: 4 deploy tasks wrapped in `module_defaults` block, owner/group removed from individual tasks (4 repetitions → 1)
- valkey/configure.yml: directory creation + template deploy tasks wrapped in `module_defaults` block with shared owner/group/mode; `state: directory` kept on tasks for readability
- openbao/configure.yml: TLS cert generation wrapped in block/rescue with partial-file cleanup; config deploy tasks get `module_defaults` for owner/group with explicit root override for backup script
- ssh_2fa/configure.yml: SSH drop-in templates in `module_defaults` block (owner/group/mode/backup shared); 4 PAM tasks consolidated into 2 named blocks with shared block-level `when` conditions

## Task Commits

1. **Task 1: module_defaults + block/rescue for cloudflare_origin_ssl, valkey, openbao** - `0e9ca1a` (refactor)
2. **Task 2: module_defaults + consolidated when blocks for ssh_2fa + OPT-06 audit** - `b527e52` (refactor)

**Plan metadata:** (docs commit — see final commit)

## Files Created/Modified

- `ansible/roles/cloudflare_origin_ssl/tasks/main.yml` - Deploy tasks in module_defaults block; owner/group removed from 4 tasks
- `ansible/roles/valkey/tasks/configure.yml` - Directory + template tasks in module_defaults block; sysctl/THP blocks outside
- `ansible/roles/openbao/tasks/configure.yml` - TLS block/rescue with cleanup; config deploy in module_defaults block
- `ansible/roles/ssh_2fa/tasks/configure.yml` - SSH drop-ins in module_defaults block; PAM tasks in 2 consolidated when blocks

## Decisions Made

- valkey: kept `state: directory` on individual tasks rather than in module_defaults — semantically important to see at a glance; only owner/group/mode in defaults
- openbao backup script: explicit `owner: root, group: root` override retained (differs from block default openbao_user/group)
- ssh_2fa PAM tasks: PAM template tasks keep explicit owner/group/mode (not inside drop-in module_defaults block) — different destination dirs, different mode (0644 vs 0600)
- OPT-06: all uri/get_url calls in nginx_wordpress/service.yml, openbao/install.yml, nginx_wordpress/install-Debian.yml, nginx_wordpress/nginx-repo.yml are isolated single-use with no shared params — no consolidation needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `ansible-lint` on full role directories (`ansible/roles/valkey/`) fails on pre-existing `tests/test.yml` role resolution error (role 'valkey' not found in tests/ role path). This is a pre-existing issue predating this plan. All 4 modified task files pass lint individually. Logged to deferred-items for 09.1.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 4 service role task files are DRY with module_defaults and/or block consolidation
- Pattern is consistent with 09.1-01 nginx_wordpress work
- 09.1-03 can proceed with remaining roles (apparmor, common, fail2ban, firewall, monitoring)

---
*Phase: 09.1-ansible-roles-optimization-module-defaults-block-consolidation-idiomatic-file-management*
*Completed: 2026-04-21*
