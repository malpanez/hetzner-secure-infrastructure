---
phase: 07-unified-deploy-yml-orchestrator
plan: 03
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified:
  - ansible/playbooks/dual-wordpress.yml
  - ansible/playbooks/tasks/openbao-bootstrap.yml
  - ansible/roles/security_hardening/templates/motd.sh.j2
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
gap_closure: true

must_haves:
  truths:
    - "Academy site gets a Let's Encrypt certificate on fresh deploy so nginx starts without error"
    - "secret/wordpress-academy KV entry contains db_name, db_user, db_password, and admin_password"
    - "MOTD shows HTTPS VAULT_ADDR and VAULT_SKIP_VERIFY so bao kv get commands work without manual correction"
  artifacts:
    - path: "ansible/playbooks/dual-wordpress.yml"
      provides: "Academy SSL via Let's Encrypt with Cloudflare DNS-01"
      contains: "nginx_wordpress_letsencrypt_enabled: true"
    - path: "ansible/playbooks/tasks/openbao-bootstrap.yml"
      provides: "Complete KV seeding for wordpress-academy"
      contains: "db_name=wordpress_academy"
    - path: "ansible/roles/security_hardening/templates/motd.sh.j2"
      provides: "Correct VAULT_ADDR with TLS and skip-verify"
      contains: "https://127.0.0.1:8200"
  key_links:
    - from: "ansible/playbooks/dual-wordpress.yml"
      to: "nginx_wordpress role certbot tasks"
      via: "nginx_wordpress_letsencrypt_enabled: true triggers certbot"
      pattern: "nginx_wordpress_letsencrypt_enabled: true"
    - from: "ansible/playbooks/tasks/openbao-bootstrap.yml"
      to: "OpenBao KV secret/wordpress-academy"
      via: "bao kv put with all 4 fields"
      pattern: "db_name=wordpress_academy db_user=wp_academy"
    - from: "ansible/roles/security_hardening/templates/motd.sh.j2"
      to: "OpenBao TLS listener"
      via: "VAULT_ADDR env var"
      pattern: "https://127.0.0.1:8200"
---

<objective>
Close 3 verification gaps found in Phase 07 code review: (1) academy SSL cert missing on fresh deploy (BLOCKER), (2) incomplete KV seeding for wordpress-academy, (3) MOTD VAULT_ADDR using HTTP instead of HTTPS.

Purpose: Without these fixes, deploy.yml fails on fresh servers (nginx cannot start without academy cert) and operators get broken credential retrieval commands from the MOTD.
Output: Three files patched with exact fixes; all grep-verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ansible/playbooks/dual-wordpress.yml
@ansible/playbooks/tasks/openbao-bootstrap.yml
@ansible/roles/security_hardening/templates/motd.sh.j2
@ansible/inventory/group_vars/wordpress_servers/nginx.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix academy SSL — enable Let's Encrypt with Cloudflare DNS-01 in dual-wordpress.yml</name>
  <files>ansible/playbooks/dual-wordpress.yml</files>
  <read_first>
    - ansible/playbooks/dual-wordpress.yml (full file — academy vars block lines 69-106)
    - ansible/inventory/group_vars/wordpress_servers/nginx.yml (SSL pattern lines 13-22)
    - ansible/roles/nginx_wordpress/defaults/main.yml (letsencrypt defaults lines 61-72)
  </read_first>
  <action>
In `ansible/playbooks/dual-wordpress.yml`, within the academy `include_role` vars block (line 69-106):

1. Replace line 90:
   BEFORE: `nginx_wordpress_letsencrypt_enabled: false`
   AFTER (5 lines replacing that 1 line):
   ```yaml
        nginx_wordpress_letsencrypt_enabled: true
        nginx_wordpress_letsencrypt_domains:
          - academy.twomindstrading.com
        nginx_wordpress_letsencrypt_cloudflare_api_token: "{{ vault_cloudflare_api_token }}"
        nginx_wordpress_ssl_cert_path: "/etc/letsencrypt/live/academy.twomindstrading.com/fullchain.pem"
        nginx_wordpress_ssl_key_path: "/etc/letsencrypt/live/academy.twomindstrading.com/privkey.pem"
   ```

This ensures certbot runs for academy before nginx tries to load the cert. The main site already has letsencrypt_enabled via group_vars (nginx.yml line 16-19). Academy needs explicit overrides because the main site's letsencrypt_domains include www/grafana/prometheus subdomains that do not apply to academy.

Do NOT add `nginx_wordpress_letsencrypt_email` — it inherits from group_vars (nginx.yml line 17).
Do NOT add `nginx_wordpress_ssl_enabled` — it inherits `true` from group_vars (nginx.yml line 13).
  </action>
  <verify>
    <automated>grep -n 'nginx_wordpress_letsencrypt_enabled: true' ansible/playbooks/dual-wordpress.yml && grep -n 'academy.twomindstrading.com/fullchain.pem' ansible/playbooks/dual-wordpress.yml && grep -n 'academy.twomindstrading.com/privkey.pem' ansible/playbooks/dual-wordpress.yml && grep -n 'vault_cloudflare_api_token' ansible/playbooks/dual-wordpress.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'nginx_wordpress_letsencrypt_enabled: true' ansible/playbooks/dual-wordpress.yml` matches (no more `false`)
    - `grep 'nginx_wordpress_letsencrypt_enabled: false' ansible/playbooks/dual-wordpress.yml` returns NO matches
    - `grep 'academy.twomindstrading.com' ansible/playbooks/dual-wordpress.yml` shows letsencrypt_domains, ssl_cert_path, and ssl_key_path
    - `grep 'vault_cloudflare_api_token' ansible/playbooks/dual-wordpress.yml` shows the API token variable
    - `ansible-lint ansible/playbooks/dual-wordpress.yml` exits 0 (or only pre-existing warnings)
  </acceptance_criteria>
  <done>Academy vars block has letsencrypt_enabled: true with domains, cloudflare token, cert_path, and key_path. No `letsencrypt_enabled: false` remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix academy KV seeding — add db_name, db_user, db_password to secret/wordpress-academy</name>
  <files>ansible/playbooks/tasks/openbao-bootstrap.yml</files>
  <read_first>
    - ansible/playbooks/tasks/openbao-bootstrap.yml (lines 219-230 for main wordpress pattern, lines 401-411 for academy task)
    - ansible/playbooks/dual-wordpress.yml (lines 79-81 for academy DB vars: wordpress_academy / wp_academy)
  </read_first>
  <action>
In `ansible/playbooks/tasks/openbao-bootstrap.yml`, find the task "Seed WordPress Academy admin password in OpenBao" (line 401-411).

Replace lines 403-404:
BEFORE:
```yaml
    {{ openbao_cli }} kv put secret/wordpress-academy
    admin_password={{ vault_wordpress_academy_admin_password }}
```

AFTER:
```yaml
    {{ openbao_cli }} kv put secret/wordpress-academy
    db_name=wordpress_academy
    db_user=wp_academy
    db_password={{ vault_wp_academy_db_password }}
    admin_password={{ vault_wordpress_academy_admin_password }}
```

Also update the `when` conditional to require the DB password variable. Replace line 408:
BEFORE:
```yaml
    - vault_wordpress_academy_admin_password is defined
```

AFTER:
```yaml
    - vault_wordpress_academy_admin_password is defined
    - vault_wp_academy_db_password is defined
```

The values `wordpress_academy` and `wp_academy` match `dual-wordpress.yml` lines 79-80 exactly. This makes `secret/wordpress-academy` consistent with `secret/wordpress` (line 219-225) which has all 4 fields.
  </action>
  <verify>
    <automated>grep -A6 'kv put secret/wordpress-academy' ansible/playbooks/tasks/openbao-bootstrap.yml | grep -c 'db_name\|db_user\|db_password\|admin_password' | grep -q 4 && echo "OK: all 4 fields present" || echo "FAIL: missing fields"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'db_name=wordpress_academy' ansible/playbooks/tasks/openbao-bootstrap.yml` matches exactly 1 line
    - `grep 'db_user=wp_academy' ansible/playbooks/tasks/openbao-bootstrap.yml` matches exactly 1 line
    - `grep 'db_password={{ vault_wp_academy_db_password }}' ansible/playbooks/tasks/openbao-bootstrap.yml` matches exactly 1 line
    - `grep 'admin_password={{ vault_wordpress_academy_admin_password }}' ansible/playbooks/tasks/openbao-bootstrap.yml` matches (already existed)
    - `grep 'vault_wp_academy_db_password is defined' ansible/playbooks/tasks/openbao-bootstrap.yml` matches in the when block
  </acceptance_criteria>
  <done>secret/wordpress-academy KV seeding includes db_name, db_user, db_password, and admin_password — matching the pattern of secret/wordpress.</done>
</task>

<task type="auto">
  <name>Task 3: Fix MOTD VAULT_ADDR — change HTTP to HTTPS and add VAULT_SKIP_VERIFY</name>
  <files>ansible/roles/security_hardening/templates/motd.sh.j2</files>
  <read_first>
    - ansible/roles/security_hardening/templates/motd.sh.j2 (line 71 for the VAULT_ADDR line)
  </read_first>
  <action>
In `ansible/roles/security_hardening/templates/motd.sh.j2`, find line 71:

BEFORE (line 71):
```
echo -e "  ${YELLOW}export VAULT_ADDR=http://127.0.0.1:8200${NC}"
```

AFTER (2 lines replacing that 1 line):
```
echo -e "  ${YELLOW}export VAULT_ADDR=https://127.0.0.1:8200${NC}"
echo -e "  ${YELLOW}export VAULT_SKIP_VERIFY=1${NC}"
```

OpenBao primary listens on HTTPS (self-signed cert). Without VAULT_SKIP_VERIFY=1, `bao` commands fail with "x509: certificate signed by unknown authority". The skip-verify is safe because it is localhost-only communication.
  </action>
  <verify>
    <automated>grep 'VAULT_ADDR=https://127.0.0.1:8200' ansible/roles/security_hardening/templates/motd.sh.j2 && grep 'VAULT_SKIP_VERIFY=1' ansible/roles/security_hardening/templates/motd.sh.j2 && ! grep 'VAULT_ADDR=http://127.0.0.1:8200' ansible/roles/security_hardening/templates/motd.sh.j2</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'VAULT_ADDR=https://127.0.0.1:8200' ansible/roles/security_hardening/templates/motd.sh.j2` matches exactly 1 line
    - `grep 'VAULT_ADDR=http://127.0.0.1:8200' ansible/roles/security_hardening/templates/motd.sh.j2` returns NO matches (http without s is gone)
    - `grep 'VAULT_SKIP_VERIFY=1' ansible/roles/security_hardening/templates/motd.sh.j2` matches exactly 1 line
    - The VAULT_SKIP_VERIFY line appears immediately after the VAULT_ADDR line
  </acceptance_criteria>
  <done>MOTD displays `export VAULT_ADDR=https://127.0.0.1:8200` followed by `export VAULT_SKIP_VERIFY=1`. No HTTP-only VAULT_ADDR remains.</done>
</task>

</tasks>

<verification>
All 3 gaps closed:

1. **GAP-1 (BLOCKER):** `grep 'nginx_wordpress_letsencrypt_enabled: false' ansible/playbooks/dual-wordpress.yml` returns nothing; academy has full Let's Encrypt config.
2. **GAP-2 (DATA):** `grep -A6 'kv put secret/wordpress-academy' ansible/playbooks/tasks/openbao-bootstrap.yml` shows all 4 fields (db_name, db_user, db_password, admin_password).
3. **GAP-3 (USABILITY):** `grep 'VAULT_ADDR=http://' ansible/roles/security_hardening/templates/motd.sh.j2` returns nothing; HTTPS and SKIP_VERIFY are present.

Lint check: `ansible-lint ansible/playbooks/dual-wordpress.yml ansible/playbooks/tasks/openbao-bootstrap.yml` exits 0 or only pre-existing warnings.
</verification>

<success_criteria>
- Academy site will obtain a Let's Encrypt cert via Cloudflare DNS-01 on fresh deploy, preventing nginx startup failure
- OpenBao KV path secret/wordpress-academy has parity with secret/wordpress (all 4 credential fields)
- Any operator SSHing into the server sees correct HTTPS VAULT_ADDR + VAULT_SKIP_VERIFY in MOTD
</success_criteria>

<output>
After completion, create `.planning/phases/07-unified-deploy-yml-orchestrator-single-command-fresh-deployment-with-auto-pause-checkpoints-for-openbao-transit-and-primary-initialization/07-03-SUMMARY.md`
</output>
