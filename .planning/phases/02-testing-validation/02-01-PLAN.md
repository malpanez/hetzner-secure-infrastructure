---
phase: 02-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/roles/nginx_wordpress/molecule/default/molecule.yml
  - ansible/roles/nginx_wordpress/molecule/default/verify.yml
  - ansible/roles/nginx_wordpress/tasks/configure.yml
  - ansible/roles/nginx_wordpress/tasks/nginx-repo.yml
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "molecule test completes with exit code 0 in nginx_wordpress role"
    - "verify.yml checks the correct web root /var/www/wordpress"
    - "nginx_wordpress_site_name is explicitly set to wordpress in molecule host_vars"
    - "ansible-lint no-handler violation on configure.yml is resolved"
    - "ansible-lint jinja[spacing] violation on nginx-repo.yml is resolved"
  artifacts:
    - path: "ansible/roles/nginx_wordpress/molecule/default/verify.yml"
      provides: "Corrected web root path assertion"
      contains: "/var/www/wordpress"
    - path: "ansible/roles/nginx_wordpress/molecule/default/molecule.yml"
      provides: "Explicit site_name in host_vars"
      contains: "nginx_wordpress_site_name: wordpress"
    - path: "ansible/roles/nginx_wordpress/tasks/configure.yml"
      provides: "Handler notification instead of inline reload"
      contains: "notify: reload nginx"
    - path: "ansible/roles/nginx_wordpress/tasks/nginx-repo.yml"
      provides: "Fixed jinja spacing"
      contains: "ansible_distribution | lower"
  key_links:
    - from: "ansible/roles/nginx_wordpress/tasks/configure.yml"
      to: "ansible/roles/nginx_wordpress/handlers/main.yml"
      via: "notify: reload nginx triggers handler"
      pattern: "notify.*reload nginx"
    - from: "ansible/roles/nginx_wordpress/molecule/default/verify.yml"
      to: "ansible/roles/nginx_wordpress/defaults/main.yml"
      via: "web root path matches role default"
      pattern: "/var/www/wordpress"
---

<objective>
Fix Molecule test failures and ansible-lint violations in the nginx_wordpress role, then run
`molecule test` to confirm the refactored role passes all stages (create, converge, idempotency,
verify, destroy).

Purpose: Satisfy TEST-01 — the refactored role must pass Molecule before proceeding to server rebuild.
Output: Green `molecule test` run; 4 files fixed with conventional commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-testing-validation/02-CONTEXT.md
@.planning/phases/02-testing-validation/02-RESEARCH.md

@ansible/roles/nginx_wordpress/molecule/default/molecule.yml
@ansible/roles/nginx_wordpress/molecule/default/verify.yml
@ansible/roles/nginx_wordpress/molecule/default/converge.yml
@ansible/roles/nginx_wordpress/tasks/configure.yml
@ansible/roles/nginx_wordpress/tasks/nginx-repo.yml
@ansible/roles/nginx_wordpress/handlers/main.yml
@ansible/roles/nginx_wordpress/defaults/main.yml
@.ansible-lint
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Molecule files and ansible-lint violations</name>
  <files>
    ansible/roles/nginx_wordpress/molecule/default/molecule.yml
    ansible/roles/nginx_wordpress/molecule/default/verify.yml
    ansible/roles/nginx_wordpress/tasks/configure.yml
    ansible/roles/nginx_wordpress/tasks/nginx-repo.yml
  </files>
  <read_first>
    ansible/roles/nginx_wordpress/molecule/default/molecule.yml
    ansible/roles/nginx_wordpress/molecule/default/verify.yml
    ansible/roles/nginx_wordpress/tasks/configure.yml
    ansible/roles/nginx_wordpress/tasks/nginx-repo.yml
    ansible/roles/nginx_wordpress/handlers/main.yml
    ansible/roles/nginx_wordpress/defaults/main.yml
  </read_first>
  <action>
    Four targeted fixes, each a separate conventional commit:

    **Fix A — molecule.yml (CONTEXT.md locked decision):**
    Add `nginx_wordpress_site_name: wordpress` to the `host_vars.nginx-wordpress-debian13` block
    in `molecule.yml`. Place it as the first variable in the block (before `nginx_wordpress_server_name`).
    This documents intent per CONTEXT.md decision "Required converge.yml update".
    Commit: `fix(molecule): add explicit site_name to molecule host_vars`

    **Fix B — verify.yml (Pitfall 1 from RESEARCH.md):**
    In `verify.yml` line 42, change `path: /var/www/html` to `path: /var/www/wordpress`.
    The role default `nginx_wordpress_web_root` is `/var/www/wordpress` — the verify assertion
    must match. Do NOT change the role default (CONTEXT.md constraint).
    Commit: `fix(molecule): correct verify.yml web root path to /var/www/wordpress`

    **Fix C — configure.yml no-handler (Pitfall 2 from RESEARCH.md):**
    1. On the "Deploy modular WordPress site configuration" task (line 140), add `notify: reload nginx`
       after the `mode: '0644'` line (before `register:`). Keep the `register: nginx_config_deployed` line.
    2. Delete lines 178-183 entirely (the "Reload Nginx if configuration is valid" task with
       `ansible.builtin.systemd` + `when: nginx_config_deployed is changed`).
    The handler `reload nginx` already exists in `handlers/main.yml`.
    Commit: `fix(nginx_wordpress): replace inline reload with handler notification`

    **Fix D — nginx-repo.yml jinja spacing (Pitfall 3 from RESEARCH.md):**
    On line 29 of `nginx-repo.yml`, change `ansible_distribution|lower` to
    `ansible_distribution | lower` (add spaces around the pipe operator).
    Commit: `fix(nginx_wordpress): fix jinja spacing in nginx-repo.yml`
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/hetzner-secure-infrastructure/ansible && ansible-lint --offline --profile production roles/nginx_wordpress/tasks/configure.yml roles/nginx_wordpress/tasks/nginx-repo.yml 2>&1 | grep -E '(no-handler|jinja\[spacing\])' && echo "LINT VIOLATIONS REMAIN" || echo "LINT CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'nginx_wordpress_site_name: wordpress' ansible/roles/nginx_wordpress/molecule/default/molecule.yml` returns 1
    - `grep -c '/var/www/wordpress' ansible/roles/nginx_wordpress/molecule/default/verify.yml` returns 1
    - `grep -c '/var/www/html' ansible/roles/nginx_wordpress/molecule/default/verify.yml` returns 0
    - `grep -c 'notify: reload nginx' ansible/roles/nginx_wordpress/tasks/configure.yml` returns 1
    - `grep -c 'Reload Nginx if configuration is valid' ansible/roles/nginx_wordpress/tasks/configure.yml` returns 0
    - `grep -c 'ansible_distribution | lower' ansible/roles/nginx_wordpress/tasks/nginx-repo.yml` returns 1
    - `grep -c 'ansible_distribution|lower' ansible/roles/nginx_wordpress/tasks/nginx-repo.yml` returns 0
  </acceptance_criteria>
  <done>All 4 fixes applied and committed. ansible-lint reports no no-handler or jinja[spacing] violations on the modified files.</done>
</task>

<task type="auto">
  <name>Task 2: Run molecule test end-to-end</name>
  <files>
    ansible/roles/nginx_wordpress/molecule/default/molecule.yml
    ansible/roles/nginx_wordpress/molecule/default/verify.yml
  </files>
  <read_first>
    ansible/roles/nginx_wordpress/molecule/default/molecule.yml
    ansible/roles/nginx_wordpress/molecule/default/converge.yml
    ansible/roles/nginx_wordpress/molecule/default/verify.yml
    CLAUDE.md
  </read_first>
  <action>
    Run `molecule test` from the nginx_wordpress role directory:

    ```bash
    cd /home/malpanez/repos/hetzner-secure-infrastructure/ansible/roles/nginx_wordpress
    molecule test
    ```

    Expected flow: create -> converge -> idempotency -> verify -> destroy.

    **If idempotency fails:** Investigate which tasks report `changed` on second run. Common cause:
    the nginx reload handler fires on first converge (expected) but should NOT fire on second.
    If a task spuriously reports changed, add `changed_when: false` or fix the conditional.
    Commit any additional fix as `fix(molecule): resolve idempotency issue in [task name]`.

    **If verify fails on a task other than the web root fix:** Read the error, check if it is a
    pre-existing issue or Phase 1 regression. Fix forward — do not revert Phase 1 changes.

    **If converge fails:** Read the full error output. The most likely causes are:
    1. Missing dependency in Docker container (fix in converge.yml pre_tasks)
    2. Template rendering error (fix in the template file)
    Do NOT add `cache_valid_time` to any apt task (CLAUDE.md constraint).

    **Timeout:** molecule test can take 5-10 minutes. Use a 600s timeout.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/hetzner-secure-infrastructure/ansible/roles/nginx_wordpress && molecule test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `molecule test` exits with code 0
    - Output contains "Idempotence completed successfully"
    - Output contains "Verifier completed successfully" (or equivalent verify pass message)
    - No `FAILED` tasks in the converge or verify stages
  </acceptance_criteria>
  <done>molecule test passes all stages (create, converge, idempotency, verify, destroy) with exit code 0. TEST-01 satisfied.</done>
</task>

</tasks>

<verification>
- `molecule test` exits 0 from `ansible/roles/nginx_wordpress/`
- `ansible-lint --offline --profile production .` from `ansible/` reports no first-party violations in modified files
- All 4 file fixes are committed with conventional commit messages
</verification>

<success_criteria>
TEST-01 is complete: `molecule test` passes in the nginx_wordpress role with the Phase 1 refactor applied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing-validation/02-01-SUMMARY.md`
</output>
